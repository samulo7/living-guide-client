```
# 🏙️ 躺平指南 (Living Guide Client)

> 一个基于 Uni-app x 开发的城市宜居评估 App，致力于帮助数字游民和旅居者找到最适合自己的“梦中情城”。

## ✨ 项目简介

本项目旨在解决旅居者在选择城市时的**信息不对称**问题。不同于传统的旅游攻略，我们更关注**长期居住**的体验，如生活成本、医疗资源、气候体感以及那些只有住久了才知道的“避坑槽点”。

## 核心功能

* **📊 城市仪表盘**：一目了然的房价、租金、交通配置。
* **🏥 医疗环境评估**：P0 级核心功能，自动识别三甲医院覆盖情况，提供不同人群的宜居建议。
* **📝 小编点评**：客观中立的城市居住体验简评。
* **⚠️ 避坑指南**：基于 UGC (用户投稿) + 人工审核机制的真实槽点展示（如供暖不足、外卖不便等）。
* **🌤️ 气候体感**：针对长期居住者的真实气候反馈。

## 🛠️ 技术栈

* **前端**：Uni-app x (Vue 3 + UTS)
* **开发工具**：HBuilderX
* **数据处理 (ETL)**：Python (Pandas)
    * 使用 `etl.py` 脚本处理 Excel 源数据。
    * 集成高德地图 API 自动获取城市坐标。
    * 实现了增量更新与变更检测报告。

## 🚀 快速开始

### 1. 前端运行
1.  下载并安装 **HBuilderX** (App开发版)。
2.  导入本项目文件夹 `living-guide-client`。
3.  点击菜单栏 `运行` -> `运行到手机或模拟器`。

### 2. 数据更新
本项目采用 **Excel + Python** 的数据管理模式。
1.  修改 `raw_data/master_data.xlsx` 中的城市数据。
2.  运行 ETL 脚本更新前端数据：
    ```bash
    python etl.py
    ```
3.  脚本会自动生成 `static/data/data.json` 并打印变更报告。

## 📂 目录结构

```text
living-guide-client/
├── pages/              # 页面文件 (首页、详情页)
├── static/             # 静态资源
│   ├── data/           # 生成的 JSON 数据
│   ├── covers/         # 城市封面图
│   └── images/         # 图标等素材
├── utils/              # 工具函数
├── components/         # 公共组件
├── etl.py              # 数据处理脚本 (核心)
└── raw_data/           # Excel 源文件目录xxxxxxxxxx ## 📝 贡献指南目前项目处于 MVP 阶段，数据更新主要依赖人工维护 `master_data.xlsx`。欢迎提交 Issue 反馈 Bug 或建议。---*Built with ❤️ by Samulo*
```