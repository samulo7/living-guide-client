# Codex 任务书：Sprint 4 - 社交全链路升级 (Master 版)

## 1. 后端：架构级数据隔离与关联
- **实时用户信息 (SQL JOIN)**: 
    - 修改 `GET /api/social/list` 和 `detail`。
    - 逻辑：通过 `companions.user_id` 联表查询 `users` 表，确保头像、昵称、签名始终为最新状态。
- **存储增强**: 
    - `companions` 表新增 `location_name` (位置名), `images` (JSON/Text 存储图片路径数组), `contact` (联系方式)。
- **时区纠偏**: 强制设置数据库连接时区为 `+08:00`，确保 `created_at` 符合北京时间。

## 2. 前端：发布页 (publish.uvue) 体验革命
- **位置锚点**: 
    - 复用 V1.0 高德 API。
    - 交互：点击位置栏进入搜索，选择后在发布页显示『📍 城市 · 具体地点』。
- **媒体管理**: 
    - 实现 `uni.chooseImage` 选择图片（上限 3 张）。
    - 增加缩略图删除按钮、点击图片预览大图逻辑。
- **表单校验**: 
    - 增加字数实时统计 (0/20, 0/200)。
    - 若标题、内容、位置任一为空，『立即发布』按钮置灰并禁用。

## 3. 前端：详情页 (companion_detail.uvue) 社交闭环
- **UI 布局**: 
    - 参考房源详情页 [image_b286a4.jpg]，顶部为图片轮播，下方为用户信息卡片和正文。
- **核心功能**: 
    - 底部 Action Bar：实现『点赞』(防抖处理) 和『想要联系』(弹出复制微信号/手机号)。
- **页面路由**: 
    - 点击广场卡片任何区域 -> 进入详情。
    - 点击详情页头像 -> 进入对应的用户个人主页。

## 4. 健壮性验收
- **无图适配**: 无图帖子自动隐藏轮播图区域，文案排版自适应。
- **数据同步**: 修改个人资料后，广场历史帖子的信息必须 0 延迟同步。