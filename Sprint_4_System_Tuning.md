# Codex 任务书：Sprint 4 - 系统调优与个人资料解锁

## 1. 任务背景
在 Sprint 3 成功打通发布逻辑后，发现时区不一致、资料无法修改、发布 UI 体验粗糙等问题。本任务旨在完成系统层面的“精装修”。

## 2. 后端开发要求 (Node.js)
- **时区纠偏**：修改数据库连接配置文件，强制设置 `timezone: '+08:00'`。确保所有写入 `companions` 表的 `created_at` 默认为北京时间。
- **资料更新接口**：
    - 路径：`PUT /api/user/profile`
    - 逻辑：接收并更新 `users` 表中的 `username`, `tagline`, `avatar` 字段。
    - 鉴权：必须经过 `authMiddleware`。

## 3. 前端开发要求 (Uniapp x / UVUE)
- **个人资料编辑页 (`pages/user/edit.uvue`)**：
    - 新建编辑页面，包含头像上传（模拟）、昵称修改、签名修改。
    - 提交成功后更新本地 `Storage` 并刷新个人中心。
- **发布页 UI 进化 (`pages/social/publish.uvue`)**：
    - 为标题和内容增加字数统计（如 0/20, 0/200）。
    - 优化标签（Tags）点击态：选中后背景色高亮并加粗，取消则恢复。
- **时间格式化工具**：
    - 在 `social.uvue` 中优化时间显示逻辑：若为今天发布，显示『今天 HH:mm』，否则显示日期。

## 4. 验收标准
- 发布后的帖子时间与本地手机时间一致。
- 修改个人资料后，个人中心顶部立即显示新昵称和新签名。
- 发布页面在输入超过字数限制时，『立即发布』按钮禁用。