# 🏕️ 躺平指南 (Nomad Guide) - V2.0 Master PRD & 架构蓝图

## 🎯 V2.0 核心里程碑 (The Vision)
V1.0 MVP 已经完美跑通了 UI 表现层和基础路由。V2.0 的核心使命是：**从“静态数据展示”彻底进化为“动态交互的真实全栈应用”**。重点攻克地理位置可视化（雷达）、真实状态机（鉴权体系）、全链路增删改查（CRUD）以及动态检索引擎。

---

## 🗺️ 阶段一：雷达复兴 (交互与动画基建) - Sprint 1
**目标**：弥补初版设计的遗憾，在【搭子广场】引入地理位置社交雷达概念。
* **功能定义**：
  * 在 `pages/social/social.uvue` 页面顶部增加「列表模式 / 雷达模式」的无缝切换 Toggle。
  * **雷达模式 UI**：全屏暗色调/科技感背景，中心固定当前用户头像，向外扩散 CSS 绿/蓝色扫描波纹动画。
  * **模拟渲染**：在波纹范围内，随机绝对定位（`position: absolute`）渲染 3-5 个附近游民的头像。点击头像弹出简易的招呼卡片。
* **技术关键**：复杂的 CSS Keyframes 动画性能优化，绝对定位的动态坐标计算。

---

## 🔐 阶段二：真理之门 (真实账号与状态管理) - Sprint 2
**目标**：废弃 V1.0 的硬编码 `user_id = 1`，建立包含 Token 的真实鉴权流。
* **后端改造 (Node.js)**：
  * 引入 `jsonwebtoken` (JWT)。
  * 提供 `POST /api/auth/login` 接口（MVP 阶段暂不支持真实手机验证码，采用输入任意测试手机号+验证码即可下发 Token 的半真逻辑）。
  * 封装 `authMiddleware` 中间件，保护私密路由（如获取收藏、发布动态）。
* **前端改造 (Uniapp)**：
  * 登录页改造为真实的表单提交。
  * 将后端的 Token 持久化存储至 `uni.setStorageSync('token', token)`。
  * 封装全局的 `request.js` 请求库，在 Headers 中自动携带 `Authorization: Bearer <token>`。
  * 拦截器：当接口返回 401 (未登录) 时，自动清空状态并跳回登录页。

---

## ✍️ 阶段三：打破只读 (双向交互与 UGC) - Sprint 3
**目标**：让用户不仅能看，还能写。真正打通数据库的 `INSERT` 和 `DELETE`。
* **功能 1：真实收藏闭环**
  * 在【安家大厅】和详情页点击“收藏”按钮，发起真实 POST 请求到 `favorites` 表。
  * 【我的】页面收藏列表与真实数据实时同步。
* **功能 2：我要找搭子**
  * 在【搭子广场】新增悬浮的「+ 发布」按钮。
  * 开发 `pages/social/publish.uvue` 表单页（包含标题、内容、标签选择）。
  * 提交后存入 `companions` 表，返回列表自动刷新。

---

## 🔍 阶段四：雷达全开 (动态检索引擎) - Sprint 4
**目标**：盘活前端的搜索框和筛选 Tag，实现后端动态 SQL 查询。
* **功能定义**：
  * 首页搜索框输入城市名，后端 `WHERE city_name LIKE '%xxx%'`。
  * 【安家大厅】顶部 Tag 点击（如“价格升序”），向后端传递 `sort=price_asc` 参数，后端动态拼接 `ORDER BY price ASC`。
  * 筛选“海景房”，后端联表查询或 `FIND_IN_SET` 匹配标签。

---

## 🤖 Codex AI 协同系统指令 (System Prompt for AI)
> **注意：此区块为 AI 辅助编程专属上下文。**
> 当开发者呼叫 Codex 执行 V2.0 任务时，请 Codex 严格遵循以下规范：
> 1. **遵循本 PRD 规划**：理解当前开发处于哪个 Sprint，不要越界修改无关模块。
> 2. **代码风格约束**：Uniapp 前端必须采用 Vue 3 `<script setup>` 组合式 API 写法；Node.js 后端保持 Express.js 的路由与控制器分离架构。
> 3. **防御性编程**：在处理列表渲染时，必须判断空数组状态；在处理后端数据插入时，必须处理 SQL 注入或参数缺失的校验。
> 4. **增量修改**：输出代码时，优先提供 Diff 或仅提供被修改的方法和 CSS 块，避免每次重写几百行的完整文件，降低合并成本。