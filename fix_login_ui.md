# 需求：美化登录页面 UI，打造现代 C 端 App 质感

## 当前问题
在 `pages/login/login.uvue` 中，UI 过于单调（纯灰白背景，呆板的输入框），不符合现代 App 的审美。

## 设计改造要求

1. **引入全屏背景 (Background)**：
   - 抛弃枯燥的纯色。给整个页面增加一个有质感的背景（建议使用全屏渐变色，例如深邃的暗蓝/暗紫渐变，或者调用一张已有的风景图作为全屏背景）。
2. **磨砂玻璃卡片 (Glassmorphism)**：
   - 把原本纯白色的登录表单区域，改造成“毛玻璃”效果的半透明卡片。
   - 使用 `rgba(255,255,255,0.1)` 或暗色半透明，加上较大圆角 (`border-radius: 20px`)。
   - 确保卡片在屏幕正中间垂直/水平居中。
3. **高颜值输入框 (Inputs)**：
   - 输入框去掉生硬的边框。
   - 采用圆润的胶囊形或带圆角的半透明背景。
   - 调整文字、占位符 (placeholder) 的颜色，使其在背景上清晰可见。
4. **质感按钮 (Buttons)**：
   - “登录”按钮使用抢眼的品牌色（如渐变蓝绿，或者纯黑带微光），增加圆角。
   - “注册”按钮可以做成幽灵按钮（透明背景，只有边框），与主按钮形成层次感。
5. **整体呼吸感 (Spacing)**：
   - 标题“欢迎回来”需要加大字号，增加上下留白，看起来更大气。