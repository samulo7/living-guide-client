# 需求：修复个人资料编辑状态的 UI 崩坏问题

## 当前问题
在 `pages/user/user.uvue` 中，进入 `isEditing` 状态后，顶部 Banner 区域的 UI 布局完全破坏了原有的美感。

## 修复要求

1. **隐藏头像路径输入**：
   - 头像 (`avatar`) 不需要输入框，在编辑状态下依然正常展示圆形头像图片即可。
2. **美化输入框 (Input UI)**：
   - 只保留“昵称 (username)”和“签名 (tagline)”两个输入框。
   - 输入框样式要精致：背景可以保持半透明黑 (`rgba(0,0,0,0.3)`)，但要有圆角 (`border-radius: 8px`)，内部文字居中，且字体颜色为白色。
   - 增加占位符 `placeholder="请输入昵称"` 等提示。
   - 增加上下间距 (margin)，不要挤在一起。
3. **美化按钮组 (Button Group)**：
   - “保存”和“取消”按钮不要紧贴着输入框，增加一些 `margin-top`。
   - 按钮要稍微大一点（增加 padding），两个按钮之间要有横向间距 (gap: 15px)。
4. **保持整体布局**：
   - 确保即使在编辑状态下，头像、输入框、按钮组依然是垂直居中对齐的，不要把头像挤到页面最顶端。