<template>
  <view class="page">
    <view class="center-wrap">
      <view class="brand-block">
        <text class="brand-logo">🏕</text>
        <text class="brand-title">躺平指南</text>
        <text class="brand-subtitle">寻找你的数字避难所</text>
      </view>

      <text class="tip">登录后开启完整游民体验</text>
      <view class="wechat-btn" @click="onWechatLogin">
        <text class="wechat-btn-text">微信一键登录</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
function onWechatLogin() {
  uni.setStorageSync('isLogged', true)
  uni.showToast({ title: '登录成功', icon: 'none' })
  setTimeout(() => {
    uni.navigateBack()
  }, 1000)
}
</script>

<style scoped>
.page {
  height: 100vh;
  background-color: #ffffff;
  justify-content: center;
  align-items: center;
  padding: 0 24px;
}

.center-wrap {
  width: 100%;
  max-width: 420px;
  align-items: center;
}

.brand-block {
  align-items: center;
  margin-bottom: 30px;
}

.brand-logo {
  font-size: 52px;
  line-height: 60px;
}

.brand-title {
  margin-top: 10px;
  font-size: 34px;
  color: #0f172a;
  font-weight: 900;
}

.brand-subtitle {
  margin-top: 8px;
  font-size: 14px;
  color: #94a3b8;
}

.tip {
  margin-bottom: 20px;
  font-size: 14px;
  color: #6b7280;
}

.wechat-btn {
  width: 100%;
  height: 52px;
  border-radius: 12px;
  justify-content: center;
  align-items: center;
  background-color: #16a34a;
}

.wechat-btn-text {
  color: #ffffff;
  font-size: 17px;
  font-weight: 800;
}
</style>
