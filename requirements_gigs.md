# 需求文档：新增“旅居搞钱 (Remote Gigs)”模块

## 1. 项目上下文 (Context)
* **当前任务**：在现有的旅居指南 App 中，新增一个独立的“搞钱/兼职”功能模块。
* **技术栈约束**：请自动分析当前项目目录结构（前端框架、后端语言、数据库类型），并遵循现有的代码风格和文件组织方式进行开发。

## 2. 核心功能概述
我们要解决旅居用户“没有收入”的焦虑。
新增一个 `Gigs`（搞钱）板块，展示远程/兼职工作机会，并提供一个核心功能：“生存指数计算器”（计算这份工资够在当前城市活多久）。

## 3. 数据存储 (Data Storage - JSON Mode)

**注意：** 本项目目前处于 MVP 阶段，不引入复杂的后端数据库。请沿用现有的“静态数据”模式。

**操作要求：**
1.  在项目的 `public/data/` (或 `src/data/`) 目录下，新建一个文件 `jobs.json`。
2.  数据结构定义如下 (Array of Objects)：

```json
[
  {
    "id": "job_001",
    "title": "远程小红书文案",
    "salary": 200,
    "salary_unit": "元/篇",
    "settlement": "日结",
    "tags": ["文案", "灵活"],
    "contact": "微信: hr_user_01",
    "survival_city_ref": "hegang", // 用于计算生存指数的基准城市ID
    "is_scam_warning": false
  },
  {
    "id": "job_002",
    "title": "兼职视频剪辑",
    "salary": 4000,
    "salary_unit": "元/月",
    "settlement": "月结",
    "tags": ["剪辑", "长期"],
    "contact": "邮箱: job@video.com",
    "survival_city_ref": "dali",
    "is_scam_warning": false
  }
]

## 4. 数据读取逻辑 (Data Fetching)
**请勿编写后端 API 接口。**
请在前端组件中（如 `GigsView.vue` 或 `src/composables/useJobs.js`）直接 import 或 fetch 这个 JSON 文件。

**逻辑要求：**
1.  **读取数据：** 加载 `jobs.json`。
2.  **模拟筛选：** 在前端实现一个简单的 filter 函数，支持根据 `tags` 过滤职位。
3.  **生存指数计算逻辑（重要）：**
    * 前提：我们假设 **总生活费 ≈ 房租 × 3** (包含吃饭、水电、网费)。
    * 公式：`生存月数 = 职位薪资 / (城市房租 × 3)`。
    * *例子*：如果不算×3，薪资3000除以房租300=10个月，这不合理；除以(300×3)=3.3个月，这才符合真实“躺平”体感。
## 5. 前端界面 (Frontend UI)

### 5.1 入口导航
* 在底部导航栏 (TabBar) 新增一项：
    * **名称**：搞钱
    * **图标**：请使用类似“钱袋”或“公文包”的 Icon
    * **高亮色**：使用 **金橙色 (#FAAD14)** 区别于现有的青色主题。

### 5.2 职位列表页 (Job List Page)
* **布局**：卡片式流布局。
* **卡片内容**：
    * 左侧：职位名称 + 标签（Pill 样式）。
    * 右侧：**薪资高亮显示** (e.g., ¥200/天)。
    * **生存指数（重点）**：在卡片底部显示一个小火苗图标 🔥，文案：“此收入约覆盖鹤岗 200% 生活费”（注：此处“鹤岗”可暂写死，或读取用户最后浏览的城市 ID）。

### 5.3 职位详情页 (Job Detail Page)
* **头部警告 (Risk Warning)**：
    * 必须在页面最顶部放置一个**红色/橙色背景**的警告框。
    * **文案**：“⚠️ 警惕诈骗：凡是要求先交押金、垫付资金、刷单的都是诈骗！平台仅做信息展示。”
* **生存力计算器 (Survival Calculator)**：
    * 这是一个可视化的进度条组件。
    * **逻辑**：`生存月数 = 职位薪资 / 城市月平均生活费`。
    * **展示**：请列出 3 个典型城市（鹤岗 ¥1000，大理 ¥2500，上海 ¥6000）的对比数据。
    * *示例*：
        * 在鹤岗：可躺平 **3.0 个月** (进度条满格 🟢)
        * 在大理：可躺平 **1.2 个月** (进度条一半 🟡)
        * 在上海：只能活 **0.5 个月** (进度条极短 🔴)

## 6. 执行步骤 (Step-by-Step Implementation)
请按以下顺序执行，确保**纯前端实现**：

1.  **数据层**：在 `public/data/` 下创建 `jobs.json` 并写入初始化数据。
2.  **逻辑层**：编写 `useJobs` (或类似) 的 Hook/Composable，实现读取 JSON 和计算“生存月数”的函数。
3.  **组件层**：
    * 创建 `JobCard.vue` (包含卡片布局 + 火苗图标)。
    * 创建 `SurvivalBar.vue` (实现三种颜色的进度条对比)。
4.  **页面层**：
    * 创建 `GigsView.vue` (搞钱页) 拼装上述组件。
    * 在 `router` 中注册新路由。
5.  **入口层**：修改底部导航栏 `TabBar`，加入“搞钱”入口（金橙色）。