# 🚀 躺平指南 App - V1.2 首页全场景筛选项拓展

## 🎯 任务目标
利用数据库中最新完善的 `landscape` (景观)、`transport` (交通) 和 `medical` (医疗) 字段，为首页新增丰富的快捷筛选项，打造极致的旅居决策体验。

## 🛠️ 具体开发需求

### 1. 前端 UI 与交互扩展 (横向滑动筛选栏)
**文件**: `pages/index/index.uvue`
* 在顶部的筛选按钮组中，除了保留“全部”、“租金<500”、“UP主实探”，请更新和新增以下按钮：
  * `🌊 海边/海景` (对应参数: `seaside`)
  * `⛰️ 靠山/山景` (对应参数: `mountain`)
  * `🏞️ 湖景/河景` (对应参数: `lake`)
  * `🚄 高铁直达` (对应参数: `highspeed`)
  * `🏥 三甲医疗` (对应参数: `hospitalA`)
* 保持现有的选中状态高亮（active）样式切换逻辑。
* 点击不同按钮时，将对应的英文参数赋值给 `filter` 变量，并发起后端请求。

### 2. 后端 SQL 逻辑打通
**文件**: `server/src/routes/city.js`
* 在原有的 `filter` 参数解析代码中，处理新增的条件分支，动态拼接 SQL 的 WHERE 条件：
    * `seaside`: `landscape LIKE '%海%' OR landscape LIKE '%海景%'`
    * `mountain`: `landscape LIKE '%山%' OR landscape LIKE '%山景%'`
    * `lake`: `landscape LIKE '%湖%' OR landscape LIKE '%河%'`
    * `highspeed`: `transport LIKE '%高铁%' OR transport LIKE '%动车%'`
    * `hospitalA`: `medical LIKE '%三甲%'`

## ✅ 验收标准
1. 前端的筛选栏变成一个功能强大的“决策面板”，包含海、山、湖、高铁、三甲等多个选项。
2. 点击不同的筛选按钮，大屏能精准利用对应的字段（landscape, transport, medical）进行过滤，并无缝刷新数据瀑布流。