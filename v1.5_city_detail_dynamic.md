# 🚀 躺平指南 App - V1.5 城市详情页静态 UI 动态化 (数据绑定)

## 🎯 任务目标
当前前端项目中已经存在一个设计精美的静态城市详情页 UI (`pages/city/detail.uvue`)。本次任务需要编写后端查询接口，并将前端这个静态页面的硬编码数据全部替换为 Vue 的响应式数据（动态绑定 MySQL 数据库中的字段）。

## 🛠️ 具体开发需求

### 1. 后端接口支持 (单条数据查询)
**文件**: `server/src/routes/city.js`
* 新增 API 路由：`GET /api/cities/:id`。
* 获取 URL 参数 `id`，执行 SQL：`SELECT * FROM cities WHERE id = ?`。
* 返回该城市的单条详细数据。

### 2. 首页卡片打通点击跳转
**文件**: `pages/index/index.uvue`
* 给城市列表的卡片元素绑定点击事件：`@click="goToDetail(city.id)"`。
* 使用 `uni.navigateTo({ url: '/pages/city/detail?id=' + city.id })` 跳转并携带参数。

### 3. 前端静态页面的“灵魂注入” (核心任务)
**文件**: `pages/city/detail.uvue`
* **生命周期与请求**：在 `onLoad(options)` 中接收 `options.id`，调用 `/api/cities/:id` 接口，将返回的数据存入响应式变量 `city`。
* **数据精准绑定 (替换原有死数据)**：
  * **顶部沉浸头图**：背景图/封面图绑定 `city.cover_image`。
  * **主标题 & 副标题**：数据库的 `name` 字段通常是“吉林延边州/珲春市”。请用 JS 拆分，大标题显示斜杠前部分（`city.name.split('/')[0]`），下方小字副标题显示斜杠后部分。
  * **生活成本 & 配套模块**：
    * 租金：替换为 `¥{{ city.rent_price }}` (如果为 0 则显示暂无数据)。
    * 买房：替换为 `{{ city.buy_price_desc }}`。
    * 交通：替换为 `{{ city.transport }}`。
  * **医疗环境评估模块**：
    * 医疗简述：绑定 `{{ city.medical }}`。
    * 适宜人群：绑定 `{{ city.suitable_crowd }}`。
  * **小编点评模块**：文本绑定 `{{ city.editor_comment }}`。
  * **气候体感模块**：文本绑定 `{{ city.landscape }} · {{ city.tags }}` (利用标签和景观字段来描述气候/环境)。
  * **本市房源推荐模块**：保持现状即可（留作 V2 阶段开发）。

## ✅ 验收标准
1. 点击首页“鹤岗”卡片，顺滑进入详情页。
2. 详情页的头图、高亮价格、小编点评全部变成鹤岗的真实数据，没有任何残留的静态占位符。