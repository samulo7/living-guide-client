# 🚀 躺平指南 App - V2.1 城市详情页接入真实房源数据

## 🎯 任务目标
当前城市详情页 (`pages/city/detail.uvue`) 底部的“本市房源推荐”使用的是本地假数据逻辑。本次任务需要新增后端的房源查询接口，并在前端将假数据替换为真实的 MySQL 接口请求。

## 🛠️ 具体开发需求

### 1. 后端新增房源接口 (RESTful 风格)
**文件**: `server/src/routes/city.js` (或者如果你有 house.js 也可以)
* 新增一个路由：`GET /api/cities/:id/houses`。
* 接收 URL 路径中的城市 `id` 参数。
* 执行 SQL 查询：`SELECT * FROM houses WHERE city_id = ? ORDER BY id DESC LIMIT 5`。
* 将查询到的房源数组作为 `data` 返回给前端。

### 2. 前端废弃 Mock 假数据逻辑
**文件**: `pages/city/detail.uvue`
* **移除假数据依赖**：删除顶部从 `../../utils/useSublets` 导入的 `loadSublets` 等相关引用。
* **删除旧函数**：删除原有的 `loadSubletRecommend` 函数。

### 3. 前端接入真实接口与数据渲染
**文件**: `pages/city/detail.uvue`
* **新增请求函数**：写一个 `fetchCityHouses(cityId)` 函数，使用 `uni.request` 请求刚写好的 `/api/cities/:id/houses` 接口。
* **数据流转**：在 `initData` 钩子中，拿到城市 id 后，调用 `fetchCityHouses(id)`，并将返回的真实房源数组赋值给 `recommendedSublets`。
* **UI 字段对齐检查**：请检查 HTML 模板中的 `v-for="room in recommendedSublets"` 渲染区块，确保绑定的字段与 MySQL `houses` 表完全一致：
  * 图片：`:src="room.cover_image"`
  * 标题：`{{ room.title }}`
  * 位置：`{{ room.district }} · {{ room.community }}`
  * 价格：`¥{{ room.price }}/月 · {{ room.min_duration }}`

## ✅ 验收标准
进入“鹤岗”的详情页时，底部能成功渲染出刚才存入数据库的 3 套真实房源（如“向阳一居室，暖气足”等），并带有正确的价格和图片。其他没有录入房源的城市（比如白城）则正常显示“暂未收录房源”的空状态。