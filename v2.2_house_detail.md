# 🚀 躺平指南 App - V2.2 单套房源详情页打通

## 🎯 任务目标
用户在城市详情页点击底部的某套房源后，跳转至独立的房源详情页 (`/pages/sublet/detail.uvue`)。该页面需展示房源的完整信息，并在底部提供“联系房东”的转化入口。

## 🛠️ 具体开发需求

### 1. 后端新增单套房源查询接口
**文件**: `server/src/routes/house.js` (新建此文件，并在 `server.js` 中引入挂载，例如 `app.use('/api/houses', houseRoutes)`)
* 新增路由：`GET /:id` (即完整路径为 `/api/houses/:id`)。
* 接收 URL 参数 `id`。
* 执行 SQL：`SELECT * FROM houses WHERE id = ?`。
* 返回单套房源的完整数据对象。

### 2. 前端新建房源详情页 & 注册路由
**文件**: `pages/sublet/detail.uvue` (新建页面) & `pages.json`
* **路由注册**：在 `pages.json` 中自动注册 `"path": "pages/sublet/detail"`，并设置合适的导航栏标题（如“房源详情”）。
* **数据请求**：在 `onLoad(options)` 中接收 `id`，调用 `uni.request` 请求 `/api/houses/:id`。

### 3. 前端 UI 渲染要求 (高颜值精装房)
**文件**: `pages/sublet/detail.uvue`
请用绝美的 CSS 实现以下结构（可参考之前城市详情页的沉浸感）：
* **顶部头图**：全屏宽度的 `cover_image`（如果是多图可以后续加 Swiper，目前先用单图铺满）。
* **价格与基础信息**：
  * 醒目的超大字号租金：`¥{{ house.price }}/月`。
  * 要求：`{{ house.min_duration }}`。
* **标题与位置**：
  * 粗体大标题：`{{ house.title }}`。
  * 地段信息：`📍 {{ house.district }} · {{ house.community }}`。
* **亮点标签**：
  * 将 `house.tags`（以逗号或顿号分隔）拆分后，渲染成彩色的背景小标签（如：集中供暖、精装修）。
* **底部固定操作栏 (吸底)**：
  * 类似城市详情页的底部，左侧加一个“收藏”按钮，右侧放一个硕大的主色调转化按钮：**“💬 立即联系房东”**。点击后弹出 Toast 提示：“模拟：正在获取房东微信...”。

## ✅ 验收标准
从鹤岗详情页点击“150元老破小”，能顺滑进入全新的房源详情页。页面展示正确的 150 元价格、对应图片和“生活便利,超低价”的彩色标签，且底部有吸底的联系按钮。