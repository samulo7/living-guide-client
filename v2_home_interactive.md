# 🚀 躺平指南 App - V2 首页交互与 UI 升级任务

## 🎯 任务目标
当前首页已经成功渲染了 240+ 条真实城市数据。本次任务需要完成**前端 UI 遮瑕与精简**，以及**前后端搜索/筛选链路**的彻底打通。

## 🛠️ 具体开发需求

### 1. 城市名称精简展示 (UX 优化)
**前端** (`pages/index/index.uvue`):
* 数据库下发的 `name` 字段通常包含详细地址（如："黑龙江鹤岗市/兴安区..."）。
* **需求**：在城市卡片的**主标题**处，使用 JS 字符串截取（例如 `city.name.split('/')[0]`），仅显示斜杠前面的城市名（如："黑龙江鹤岗市"）。完整的名字保留在数据对象中，供未来详情页使用。

### 2. 视觉遮瑕 (数据脱水)
**前端** (`pages/index/index.uvue`):
* **租金过滤**：遇到 `rent_price` 为 `0` 时，不要显示“¥0/月”，请使用 `v-if` 改为显示“暂无租金数据”，或者让该价格高亮区域不显示。
* **买房描述过滤**：遇到 `buy_price_desc` 的值为 `//` 或空字符串时，隐藏卡片上的买房描述区域。

### 3. 顶部搜索框打通 (前后端联动)
**前端** (`pages/index/index.uvue`): 
* 给顶部的搜索输入框绑定 `v-model`。
* 监听回车或搜索按钮点击事件，将输入的 `keyword` 作为参数调用后端的获取城市列表接口。
**后端** (`server/src/routes/city.js`): 
* 修改获取列表的接口，支持接收 `keyword` 查询参数。
* 如果有 `keyword`，在 SQL 中追加 `WHERE name LIKE '%keyword%'` 进行模糊搜索。

### 4. 黄金筛选项打通 (前端快捷按钮)
**前端** (`pages/index/index.uvue`): 
* 为“全部”、“租金<500”、“海边城市”、“UP主实探”按钮绑定点击事件，并实现选中状态的高亮（active 类名切换）。
* 点击时，向后端发送对应的筛选参数（如 `filter=under500` 等）。
**后端** (`server/src/routes/city.js`):
* 解析 `filter` 参数，动态拼接 SQL 的 WHERE 条件：
    * `under500`: `rent_price > 0 AND rent_price <= 500`
    * `seaside`: `tags LIKE '%海景%' OR tags LIKE '%海边%'`
    * `verified`: `is_verified = 1`

## ✅ 验收标准
1. 卡片大标题变得清爽简短，长地址被成功切断。
2. 列表中再也看不到刺眼的 `¥0` 和 `//`。
3. 搜索和快捷筛选项能够正确地让大屏数据动态刷新。