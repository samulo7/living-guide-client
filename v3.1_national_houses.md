# 🚀 躺平指南 App - V3.1 全国安家大厅 (房源瀑布流)

## 🎯 任务目标
打破单一城市的限制，建立一个全局的房源列表页（后续可作为 App 的主 Tab 之一）。该页面需要将全国的房源汇总，并强制按照**价格从低到高（极致性价比）**进行排序展示。

## 🛠️ 具体开发需求

### 1. 后端新增全局房源接口
**文件**: `server/src/routes/house.js`
* 新增路由：`GET /` (即完整路径为 `/api/houses`)。注意要写在 `GET /:id` 路由的前面，防止被拦截！
* 执行 SQL 联合查询：
  `SELECT h.*, c.name as city_name FROM houses h LEFT JOIN cities c ON h.city_id = c.id ORDER BY h.price ASC LIMIT 50`
* 排序逻辑：必须是 `ORDER BY h.price ASC`（价格从低到高排）。
* 返回包含 `city_name` 的房源数组。

### 2. 前端新建【安家大厅】页面
**文件**: `pages/sublet/list.uvue` (新建页面) & `pages.json`
* **路由注册**：在 `pages.json` 中注册该页面，设置导航栏标题为“全国安家大厅”或“全网捡漏”。
* **数据请求**：在 `onLoad` 中调用 `uni.request` 请求 `/api/houses`，并将数据存入 `houseList` 响应式变量。

### 3. 前端 UI 渲染要求 (清爽双列瀑布流 / 极简大列表)
**文件**: `pages/sublet/list.uvue`
* 页面顶部可以加一个简单的标语：`"全网低价房源，从这里开始躺平"`。
* 列表渲染 `v-for="room in houseList"`。
* 卡片设计必须包含：
  * **封面大图**：`room.cover_image`
  * **抢眼价格**：红色的 `¥{{ room.price }}/月`
  * **城市与位置**：`📍 {{ room.city_name }} · {{ room.district }}` (必须有城市名，因为是全国大乱炖)
  * **房源标题**：`{{ room.title }}` (最多显示两行，超出省略)
* **交互跳转**：点击任意一套房源卡片，顺滑跳转到我们刚刚封卷的详情页 `uni.navigateTo({ url: '/pages/sublet/detail?id=' + room.id })`。

## ✅ 验收标准
进入 `/pages/sublet/list` 页面后，能看到咱们之前录入的鹤岗房源（150块的老破小必须排在 350块和450块的前面！），并且带有城市名“鹤岗市”，点击可以直接进入详情页。