# 💰 躺平指南 App - V3.2 搞钱副本 (职位大厅)

## 🎯 任务目标
为数字游民和旅居人群提供生存保障。在底部的【搞钱】Tab 中，开发一个职位信息流，展示当地兼职、包吃住的义工以及完全可以远程的线上工作。

## 🛠️ 具体开发需求

### 1. 后端新增职位接口
**文件**: `server/src/routes/job.js` (新建文件) & 后端入口文件 (如 `app.js` 或 `server.js`)
* **新建路由文件**：实现 `GET /` 接口。
* **数据查询**：从 `jobs` 表查询数据。
* **关联表 (JOIN)**：必须 `LEFT JOIN cities c ON jobs.city_id = c.id`，查出 `city_name`。
* **排序规则**：按发布时间倒序排列 `ORDER BY jobs.created_at DESC`。
* **路由挂载**：在主入口文件里，将此路由挂载到 `/api/jobs`。

### 2. 前端开发【搞钱大厅】页面
**文件**: `pages.json` 里的对应页面 (假设为 `pages/job/index.uvue`，如果没有请新建并配置 tabBar 映射)
* **数据请求**：页面 `onLoad` 时请求 `/api/jobs`。
* **UI 头部设计**：
  * 大标题：`生存大厅` 或 `搞钱路子`
  * 副标题：`不赚钱怎么躺平？搞点伙食费`
* **卡片流设计 (职位列表)**：
  * 遍历渲染 `jobList`。
  * **职位名称** (`title`)：加粗大字号。
  * **薪资待遇** (`salary`)：必须高亮（如橘色字体，类似房租价格）。
  * **位置状态**：如果 `is_remote == 1`，显示一个绿色的【🌐 可远程】标识；否则显示 `📍 {{ city_name }}`。
  * **雇主信息** (`company`)：浅色小字。
  * **福利标签** (`tags`)：按逗号或顿号分割，渲染成彩色小标签（类似房源亮点）。
  * **职位描述** (`description`)：最多显示两行，超出显示省略号。

## ✅ 验收标准
点击底部的【搞钱】Tab，能丝滑加载出“网吧夜班网管”和“短视频切片”等假数据，且薪资高亮、远程标识准确。****